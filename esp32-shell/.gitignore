# ESP-IDF Build Output
# ============================================================================
# These files are generated by the ESP-IDF build system and should not be
# committed to version control.
# ============================================================================

# Build directory (contains all compiled objects and binaries)
build/

# Generated sdkconfig (use sdkconfig.defaults for version control)
sdkconfig
sdkconfig.old

# Component manager files (dependencies are downloaded on build)
managed_components/
dependencies.lock

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
